apiVersion: v1
kind: Service
metadata:
  name: nginx-chart-service01
spec:
  type: NodePort
  selector:
    app: nginx-chart-deploy01
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30002