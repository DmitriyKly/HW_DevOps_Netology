apiVersion: v1
kind: Service
metadata:
  name: nginx-multitool-service-node-port
spec:
  type: NodePort
  selector:
    app: nginx-multitool-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080